import React from "react";

const AppDashedLineSpacer = () => {
  return (
    <div
      style={{
        borderTop: "1px dashed #ccc",
        marginTop: "10px",
        marginBottom: "10px",
      }}
    ></div>
  );
};

export default AppDashedLineSpacer;
